import { RoutePlaceholder } from "@/components/layout/route-placeholder"

export default function TaxesPeruPage() {
  return (
    <RoutePlaceholder
      routeKey="taxesPeru"
      access="protected"
      protectedRoute
    />
  )
}
