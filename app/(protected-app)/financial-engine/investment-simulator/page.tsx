import { RoutePlaceholder } from "@/components/layout/route-placeholder"

export default function InvestmentSimulatorPage() {
  return (
    <RoutePlaceholder
      routeKey="investmentSimulator"
      access="protected"
      protectedRoute
    />
  )
}
